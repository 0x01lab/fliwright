# Domain Docs

This repository uses a single shared domain context.

Before exploring a domain area, read the root `CONTEXT.md` and only the ADRs in `docs/adr/` that affect that area. Use the glossary's canonical terms in issue titles, proposals, and tests. Surface any conflict with an ADR rather than silently overriding it.
