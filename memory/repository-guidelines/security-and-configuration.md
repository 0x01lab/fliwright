# Security And Configuration

Do not commit generated build output such as `dist`, Flutter generated files,
local VM service URLs, or device-specific configuration. Keep test fixtures
deterministic and avoid embedding secrets in docs, examples, or generated tests.
